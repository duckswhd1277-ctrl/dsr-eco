# soul.md - HEAT NO 자동 매칭 도구

## 🎯 프로젝트의 영혼

**철강 제조 현장에서 생산 데이터와 열처리 데이터를 자동으로 매칭하여 HEAT NO를 입력하는 도구**

- 주요 용도: 정리파일(수출).xls에 HEAT NO 자동 입력
- 목표: 현장 담당자의 반복적인 수작업 제거 및 오류 최소화
- 운영 방식: 단일 HTML 파일, 브라우저에서 직접 실행

---

## 🏗️ 아키텍처

### 기술 스택
- **Frontend**: HTML5 + CSS3 + Vanilla JavaScript (라이브러리 없음)
- **파일 처리**: ExcelJS 라이브러리 (XLSX 읽기/쓰기)
- **저장소**: 브라우저 메모리 (임시), 다운로드 파일 (결과)
- **배포**: GitHub Pages 또는 로컬 파일 접근
- **호환성**: Chrome, Edge, Firefox, Safari

### 왜 이 기술을 선택했나?
1. **단일 파일**: 배포 간단, 설치 불필요
2. **오프라인 작동**: 인터넷 연결 불필요
3. **ExcelJS 활용**: 복잡한 Excel 형식 처리 가능
4. **빠른 반응**: 브라우저 네이티브 성능

### 파일 구조
```
heat_no_mapper.html (600줄+)
├── CSS (200줄): UI, 미리보기 테이블, 다운로드 버튼 스타일
├── HTML (100줄): 파일 입력, 분석 버튼, 미리보기, 다운로드 옵션
└── JavaScript (300줄): 파일 읽기, 매칭 로직, 파일 쓰기
```

---

## 📋 핵심 기능

### 1. 3단계 매칭 로직
- **1단계**: 강종 + 선경 + ROD MAKER(포스코) 매칭 (가장 정확)
- **2단계**: 제조번호 기반 매칭 (폴백)
- **3단계**: 원재료 조회 파일 검증 (추가 확인)
- **결과**: 매칭된 HEAT NO를 정리파일에 자동 입력

### 2. 스마트 필터링
- **WL 제외**: 작업번호 'WL'로 시작하는 항목 자동 제외
- **숫자 정규화**: 강종 비교 시 숫자만 추출 (SAE9254 = 9254)
- **선경 오차 허용**: ±0.5mm 범위에서 매칭
- **중복 방지**: 같은 제조번호의 모든 행 처리

### 3. 사용자 친화적 결과 확인
- **미리보기**: OT 열처리 기준 테이블 표시
- **정렬**: 제조번호, 작업번호, 선경, 거래처, HEAT NO 컬럼 정렬 가능
- **상태 표시**: ✅ 정리파일에 존재 / ⚠️ 신규 항목
- **선택적 다운로드**: 전체 파일 또는 결과만 다운로드

---

## 🛠️ 개발 원칙

### 코딩 규칙
1. **함수 이름**: camelCase (readExcel, matchData, downloadFile)
2. **변수 이름**: camelCase (heatMapping, processedRows)
3. **최대 길이**: 함수 80줄, 파일 600줄
4. **주석**: WHY만 작성 (WHAT은 코드가 설명)

### 구조 원칙
1. **단일 책임**: 각 함수는 한 가지만 수행
   - readExcel: 파일 읽기만
   - matchData: 매칭 로직만
   - downloadFullFile/downloadMatchesOnly: 다운로드만

2. **명확한 이름**: 함수명으로 의도 명확
   - filterWLRows (WL 제외)
   - normalizeGrade (강종 정규화)
   - validateMatch (검증)

3. **최소 추상화**: 필요한 만큼만 구현
4. **순차적 흐름**: 읽기 쉬운 순서로 정렬

### 에러 처리
```javascript
// 파일 검증
- 파일 형식 확인 (.xls, .xlsx, .csv)
- 필수 컬럼 존재 확인
- 데이터 무결성 검증

// 사용자 안내
- 명확한 에러 메시지
- 해결 방법 제시
```

---

## 🚫 피해야 할 패턴

### ❌ 하지 말아야 할 것
1. **외부 라이브러리 추가** (jQuery, Bootstrap 등)
   - 이유: ExcelJS 외에 추가 의존성 회피
   - 대안: 순수 CSS/JavaScript로 구현

2. **서버 의존성** (데이터베이스, API 등)
   - 이유: 설치 및 배포 복잡화
   - 대안: 클라이언트 사이드 처리

3. **복잡한 상태 관리** (Redux 등)
   - 이유: 단순 도구에는 오버엔지니어링
   - 대안: 단순 JavaScript 객체

4. **과도한 주석**
   - 이유: 코드 가독성 저하
   - 대안: 명확한 함수명으로 자설명적 코드

5. **미사용 코드** (dead code)
   - 이유: 유지보수 혼동
   - 대안: 사용하지 않으면 즉시 삭제

---

## 📊 UI/UX 원칙

### 디자인 철학
1. **간결함**: 파일 선택 → 분석 → 다운로드 (3단계)
2. **명확성**: 버튼의 동작이 명확함
3. **반응형**: 모든 기기에서 최적화
4. **접근성**: 키보드 네비게이션 지원

### 색상 규칙
```
주요 버튼: 파란색 (#3b82f6)
성공 상태: 녹색 (#10b981)
경고 상태: 주황색 (#f59e0b)
에러 상태: 빨간색 (#ef4444)
```

### 반응형 기준
```
Desktop: 1024px 이상
Tablet:  768px ~ 1023px
Mobile:  480px ~ 767px
```

---

## 📈 워크플로우

### 개발 프로세스
```
1. Issue 등록 (GitHub) - 기능 또는 버그 보고
2. 요청: "skill로 해결해줄래?"
3. 코드 수정 (HTML/CSS/JS)
4. 로컬 테스트 (브라우저에서 heat_no_mapper.html 실행)
5. Commit & Push
6. Issue closed
```

### 사용자 실행 흐름
```
heat_no_mapper.html 브라우저 열기
    ↓
파일 선택 (정리파일.xls, OT열처리.xls 등)
    ↓
"📊 파일 분석하기" 클릭
    ↓
자동 매칭 실행
    ↓
미리보기 테이블 확인
    ↓
"💾 결과 다운로드" 클릭 (2가지 옵션)
    ↓
정리파일에 HEAT NO 입력 완료
```

---

## 🔮 향후 확장 가능성

### Phase 2: 고급 기능
- 매칭 규칙 커스터마이징 UI
- 배치 처리 (여러 파일 동시 처리)
- 매칭 로그 저장 및 추적

### Phase 3: 웹 애플리케이션
- 웹 서버 기반 배포
- 사용자 계정 및 권한 관리
- 매칭 이력 데이터베이스

---

## 📝 최종 철학

> **"현장 담당자의 반복 작업을 자동화한다"**

- 3단계 매칭으로 정확성 확보
- 명확한 미리보기로 신뢰성 향상
- 간단한 클릭만으로 결과 생성
- 설치 없이 브라우저에서 즉시 실행

---

**마지막 업데이트**: 2026년 6월 12일  
**프로젝트 상태**: 활발히 개선 중 (이슈 기반 개발)
